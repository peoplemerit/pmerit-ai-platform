{
  "name": "pmerit-ai-platform",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "dev:extension": "pnpm --filter @aixord/extension dev"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
